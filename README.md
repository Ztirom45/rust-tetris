# rust-tetris
a tetris Clone written in Rust

## specials:
- a modular backend with a mostly replaceable SDL frontend

## TODO / Issues:
- remove multiple row at once (especially not rows, which are on top of each other)
- sometimes: rotation?
